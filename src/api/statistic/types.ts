export type TotalWorkoutItem = {
  yearMonth: string;
  totalWorkout: number;
};

export type TotalSubscriptionItem = {
  yearMonth: string;
  totalSubscription: number;
};

export type TotalMenuItem = {
  yearMonth: string;
  totalMenu: number;
};

export type TotalAdvisor = {
  totalAvisor: number;
  totalMember: number;
};

export type TotalCommissionSummary = {
  yearMonth: string;
  totalCommission: number;
};
